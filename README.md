# Local Jungle Flex Masonry

To use this plugin simply install and activate it.  Then put this CSS class selector on the parent node you want to transform the children to layout in masonry for.

```html
.masonry_gallery
```